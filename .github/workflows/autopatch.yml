name: Auto Patch Rom

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@patchless
        with:
          node-version: '24.x'
      - name: testing
        env:
          USER_ID:       ${{ secrets.USER_ID }}
          CLIENT_ID:     ${{ secrets.CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
        run: |
          echo howdy lmao
          npm install box-node-sdk
          node -v
          npm -v
          node ./auto-patcher-node/upload.js
