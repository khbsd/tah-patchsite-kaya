name: Auto Patch Rom

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: testing
        run: |
          echo howdy lmao
          \. "$HOME/.nvm/nvm.sh"
          nvm install 24
          npm install box-node-sdk
          node -v
          npm -v
