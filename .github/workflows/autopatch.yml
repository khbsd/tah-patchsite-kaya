name: Auto Patch Rom

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: testing
        run: |
          echo howdy lmao
          wget -qO- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
          nvm install 24
          node -v
          npm -v
